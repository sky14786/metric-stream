# Codex 작업 규칙

세션을 시작하면 `CLAUDE.md`와 `PROGRESS.md`를 순서대로 읽습니다. `CLAUDE.md`의 프로젝트
배경, 아키텍처와 코딩 컨벤션은 이름과 무관하게 Codex에도 적용됩니다.

## 공통 작업 원칙

- WSL 네이티브 경로를 쓰고 Java·Node 실행과 서비스 검증은 Docker로 수행합니다.
- 가정을 밝히고, 요청 범위의 최소 변경만 하며, 기존 스타일을 유지합니다.
- 시크릿과 실제 `.env` 파일을 커밋하지 않습니다.
- 사용법·아키텍처가 바뀌면 README를, 완료·남은 작업이 바뀌면 `PROGRESS.md`를 갱신합니다.
- 저장소의 관련 테스트 또는 검증 명령을 실행한 뒤 변경 파일만 Conventional Commit
  형식으로 커밋합니다. `main`에 직접 푸시하지 않습니다.

Claude 전용 도구나 절차가 문서에 나오면 Codex의 동등한 기능으로 목적과 안전 조건을
유지합니다. 대응 기능이 없으면 임의로 대체하지 말고 보고합니다.
