apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    url: postgres:5432
    user: metric
    secureJsonData:
      password: metric1234
    jsonData:
      database: metricdb
      sslmode: disable
      maxOpenConns: 10
      maxIdleConns: 10
      connMaxLifetime: 14400
      postgresVersion: 1600
    isDefault: true
    editable: true
