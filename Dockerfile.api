FROM gradle:8.8-jdk17-alpine AS build
WORKDIR /workspace
COPY . .
RUN gradle :metric-api:bootJar -x test --no-daemon

FROM eclipse-temurin:17-jre-alpine
WORKDIR /app
COPY --from=build /workspace/metric-api/build/libs/*.jar app.jar
ENTRYPOINT ["java", "-Xmx128m", "-Xms64m", "-jar", "app.jar"]
